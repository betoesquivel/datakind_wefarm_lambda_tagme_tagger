# datakind_wefarm_lambda_tagme_tagger
Python AWS Lambda function that reads 10 message indexes (row in a csv) and gets them, tags them by calling the TagMe entity linker and uploads annotations to dynamodb.
